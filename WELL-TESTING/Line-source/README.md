this code only works on the version which is available in development branch
to access development branch, please install it using the whl file available in installation/DARTS_DEV

cd to this directory (Line-source) and run the following command in yout terminal
python -m package.Main